export const mountGame = (containerId: string) => {
  // TODO: Initialize Phaser game instance here
  console.log(`Mounting Tetris game to ${containerId}`);
};

export const unmountGame = () => {
  // TODO: Cleanup Phaser game instance
  console.log("Unmounting Tetris game");
};
